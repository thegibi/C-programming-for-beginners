#include <stdio.h>

int main() {
  
    int primaryNumber = 25;
    printf("Primary number is: %d", primaryNumber);

    int secondNumber = primaryNumber;
    printf("\nsecondNumber is: %d", secondNumber);
    
    return 0;
}